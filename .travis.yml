language: minimal

services:
  - docker

before_install:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin

script:
  - docker build . -f Dockerfile -t $DOCKER_USERNAME/the_doctor:latest

after_success:
  - docker push $DOCKER_USERNAME/the_doctor:latest
